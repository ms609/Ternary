#' @param nlevels,levels,zlim,\dots parameters to pass to
#'  \code{\link[graphics]{contour}()}.
#' @param color.palette parameters to pass to
#'  \code{\link[graphics]{.filled.contour}()}.
#' @param fill.col Sent as `col` parameter to 
#'  \code{\link[graphics]{.filled.contour}()}.
#'  Computed from `color.palette` if not specified.
#' @param filled Logical; if `TRUE`, contours will be filled
#'  (using \code{\link[graphics]{.filled.contour}()}.).
