% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Holdridge.R, R/Lexis.R, R/TernaryPlot.R
\name{HoldridgeToXY}
\alias{HoldridgeToXY}
\alias{LexisToXY}
\alias{CoordinatesToXY}
\title{Convert user-specified ternary coordinates into X and Y coordinates}
\usage{
HoldridgeToXY(pet, prec)

LexisToXY(
  a = NULL,
  b = NULL,
  c = NULL,
  aRange = NULL,
  bRange = NULL,
  cRange = NULL,
  direction = getOption("ternDirection", 1L)
)

CoordinatesToXY(coordinates)
}
\arguments{
\item{pet, prec}{Numeric vectors giving \emph{p}otential \emph{e}vapo\emph{t}ranspiration
ratio and annual \emph{prec}ipitation (in mm).}

\item{a, b, c}{Numeric vectors of Lexis coordinates.
Provide exactly two of the three: the two axes that were bounded
when creating the plot.  If ranges are not supplied and a Lexis plot
is active, ranges are read from the plot state; otherwise the
default \eqn{[0, 1]} range is used with axis \code{a} as the free
(derived) axis.}

\item{aRange, bRange, cRange}{Numeric vectors of length 2 giving the
range of each axis, or \code{NULL}.  Supply ranges for the same two
axes that were bounded in the corresponding \code{\link[=LexisPlot]{LexisPlot()}} call.}

\item{coordinates}{A list, matrix, data.frame or vector in which each
element (or row) specifies the three coordinates of a point in ternary space.
Each element (or row) will be rescaled such that its entries sum to 100.}
}
\value{
\code{CoordinatesToXY()} returns an array of two rows, corresponding to
the X and Y coordinates of \code{coordinates}.
}
\description{
Accepts various formats of input data; extracts ternary coordinates and
converts to X and Y coordinates.
}
\section{Functions}{
\itemize{
\item \code{HoldridgeToXY()}: Convert from Holdridge coordinates

\item \code{LexisToXY()}: Convert from Lexis coordinates

}}
\examples{
LexisToXY(b = 0.5, c = 0.3)
XYToLexis(0.05, 0.52, bRange = c(0, 1), cRange = c(0, 1))

}
\seealso{
Other Lexis plotting functions: 
\code{\link{AddToTernary}()},
\code{\link{ColourLexis}()},
\code{\link{LexisContour}()},
\code{\link{LexisPlot}()},
\code{\link{LexisPointValues}()}
}
\author{
\href{https://orcid.org/0000-0001-5660-1727}{Martin R. Smith}
(\href{mailto:martin.smith@durham.ac.uk}{martin.smith@durham.ac.uk})
}
\concept{Lexis plotting functions}
\keyword{internal}
